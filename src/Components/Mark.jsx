import React from "react";

function Mark({ handleMarkButtonClick }) {
  return (
    <button onClick={handleMarkButtonClick} className="butc">
      "
    </button>
  );
}

export default Mark;
